const addItem = document.querySelector('.add-item')//from
const itemsList = document.querySelector('.paltes') // 列表

function addItem(e){
  e.preventDefault();
  alert("阻止了")
}

addTtems.addEventListener('submit',addItem)